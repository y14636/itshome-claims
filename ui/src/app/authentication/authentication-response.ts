export interface AuthenticationResponse {
    access_token: string;
    consented_on: string;
    expires_in: string;
    id_token: string;
    refresh_token: string;
    refresh_token_expires_in: string;
    scope: string;
    test: string;
    token_type: string;
}